[project]
name = "content-parser"
version = "0.1.0"
description = "A CLI interface to make your string parsing functions accessible through command line conveniently."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
